@model IEnumerable<SporTakip.Models.WorkoutProgram>

<div class="container mt-4">
    <h2 class="text-primary fw-bold mb-4">📋 Antrenman Programlarım</h2>

    <div class="row mb-4">
        <div class="col-md-6 mb-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h5 class="card-title">Yeni Program Oluştur</h5>
                    <form asp-action="Create" method="post" class="d-flex gap-2">
                        @Html.AntiForgeryToken()
                        <input type="text" name="name" class="form-control" placeholder="Örn: Pazartesi (Göğüs/Ön Kol)" required>
                        <button type="submit" class="btn btn-success">Oluştur</button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-md-6 mb-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h5 class="card-title">Hazır Spor Programları</h5>
                    <p class="text-muted small mb-3">
                        Full body, itiş/çekiş/bacak veya upper/lower split programlarını otomatik oluştur.
                    </p>
                    <form asp-action="CreateTemplate" method="post" class="d-flex flex-column gap-2">
                        @Html.AntiForgeryToken()
                        <button type="submit" name="template" value="FullBody3" class="btn btn-outline-light btn-sm">
                            🧱 Full Body (Haftada 3 Gün)
                        </button>
                        <button type="submit" name="template" value="PushPullLegs" class="btn btn-outline-light btn-sm">
                            💥 Push / Pull / Legs (Haftada 3 Gün)
                        </button>
                        <button type="submit" name="template" value="UpperLower4" class="btn btn-outline-light btn-sm">
                            🧱 Upper / Lower (Haftada 4 Gün)
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        @foreach (var program in Model)
        {
            <div class="col-md-6 mb-3">
                <div class="card shadow h-100">
                    <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">@program.Name</h5>
                        <form asp-action="Delete" asp-route-id="@program.Id" method="post" class="d-inline">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-sm btn-outline-danger">Sil</button>
                        </form>
                    </div>
                    <div class="card-body">
                        <p class="text-muted">
                            @program.Items.Count Hareket
                        </p>
                        <ul class="list-group list-group-flush mb-3">
                            @foreach(var item in program.Items.Take(3))
                            {
                                <li class="list-group-item p-1 border-0 small">
                                    • @item.Exercise.Name (@item.DefaultSets x @item.DefaultReps)
                                </li>
                            }
                            @if(program.Items.Count > 3) { <li class="small text-muted ps-2">... ve dahası</li> }
                        </ul>

                        <div class="d-grid gap-2">
                            <form asp-action="ApplyToLog" asp-route-id="@program.Id" method="post" class="d-grid">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-primary">
                                    ▶️ Bugüne Uygula
                                </button>
                            </form>
                            <a asp-action="Details" asp-route-id="@program.Id" class="btn btn-outline-secondary btn-sm">
                                ✏️ Düzenle / Hareket Ekle
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>